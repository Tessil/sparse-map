# This module sets the following variables:
# * tsl_sparse_map_FOUND - true if tsl_sparse_map found on the system
# * tsl_sparse_map_INCLUDE_DIRS - the directory containing tsl_sparse_map headers
@PACKAGE_INIT@

if(NOT TARGET tsl::sparse_map)
  include("${CMAKE_CURRENT_LIST_DIR}/tsl_sparse_mapTargets.cmake")
  get_target_property(tsl_sparse_map_INCLUDE_DIRS tsl::sparse_map INTERFACE_INCLUDE_DIRECTORIES)
endif()
